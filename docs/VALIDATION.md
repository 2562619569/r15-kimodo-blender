# R15 Kimodo 运行验证

日期：2026-09-09。验证通过 Windows 127.0.0.1:9876 的 Blender MCP，在 Blender 5.1.1 内实际执行。

## 实际服务生成

- Proscenium 0.4.0，自托管 Kimodo `kimodo-soma-rp`，SOMA30。
- live `/capabilities` 读取成功。
- 输入：当前 `__girl_Armature` 的第 1、37、73 帧，24 FPS。
- 两条文本分别要求抬右手挥手、放下右手站立。
- 每段独立提交一条文本和两个全身姿势约束；对应模型局部第 0、45 帧，共 46 个采样点。
- 两次真实 `/generate` 请求成功，两个 glTF 响应已保存在交付目录 `generation_evidence.json`。
- 输出：73 帧、3 秒的独立 R15 Action。
- 多文本一次生成曾出现锚点附近突跳，改为独立端点约束请求；加入 0.08 秒平滑后，测试挥手片段最大逐帧身体关节旋转约 22.24 度。
- 锚点通过 Blender 实际 Action 求值验证；位置误差小于 0.000001 Blender 单位。

## 运行回归

- 当前角色源→目标往返转换：初始姿态位置、旋转误差为零。
- 独立临时测试骨架：XYZ Euler，25 FPS，起点第 11 帧，锚点 11、42、89，对应模型帧 0、37、94。
- 上述时间/旋转测试复用了真实响应并调整响应时间戳，没有声称额外调用模型。
- Euler 锚点最大旋转误差约 0.00000054 rad，位置误差小于 0.000001 单位。
- 原始 Action 与结果 Action 切换成功，重新读取关键帧保留已有分段文本。
- 空文本明确拒绝，原始/活动 Action 不变。
- `.blend` 保存并重开成功，角色引用、原始与结果 Action、两条文本和平滑参数均恢复。
- Blender 播放操作实际启动；两秒后定时观察为 playing=true / frame=48。
- 最终插件错误状态为空。

## Roblox 导出

使用本机现有 Roblox Animations Importer/Exporter 的真实 `serialize()`，成功生成 73 帧 / 3 秒 / 24 FPS 数据，并按其 `.rbxanim` 格式（UTF-8 JSON + zlib）写入交付文件。

现有导出器提示未提供 Studio 目标 rest 校准数据。本轮没有在 Roblox Studio 导入或播放验收，不能把 Blender 导出成功等同于 Studio 内播放已验证。

## 仓库交付

插件源码位于 `r15_kimodo/`，安装包位于 `dist/r15_kimodo.zip`。上述测试角色、示例场景和真实接口证据保留在原本地交付目录，不随本仓库发布。运行验证发生在仓库整理之前；发布整理未更改插件 Python 源码。

# 1.1 原始动画可视化与 IK / Reach 验证

日期：2026-09-09，Blender 5.1.1，经 Windows Blender MCP 实际操作。

- 原始 SOMA30：保存两段真实服务响应及其元数据到 blend Text 数据块，保留 91 个原始采样点（通过子帧换算到 24 FPS 时间轴）。
- 原生骨架的实际求值与原始响应 FK 对照：第 25 帧最大关节位置误差 0.000000783 Blender 单位。
- 29 条彩色父子连线与所选骨骼轨迹随时间轴播放；关闭仅隐藏源可视化，并排及叠加位置切换通过。
- 16 个调节项：15 个 R15 身体部位及 HumanoidRootPart。
- 默认参数复制原生成动作，最大矩阵分量误差 0.000000715。
- 两节右手 IK 测试：移动约 0.23461 单位，目标距离约 0.00121；位移强度 50% 时，位移约 0.11728 单位。
- IK 旋转跟随误差约 0.000000646 rad；旋转强度为零时匹配原始关键姿势 Action 的局部旋转，误差约 0.000000060 rad。
- 位移强度为零时匹配原始局部位移。锁帧开关验证通过；关闭锁定后角色根在关键帧可按目标移动 0.1 单位。
- 保持锁定时，关键姿势误差小于 0.000001 单位；锚点附近用 0.2 秒缓入缓出。
- 直接修改目标球位置（模拟 G 拖动），没有额外修改面板参数或手动调用 Apply：自动排队并烘焙成功。
- 生成完成路径使用此前真实 HTTP 响应验证：清理旧调节器，嵌入新响应，并自动重建可视化成功。本轮没有重复请求模型。
- 保存重开：原始响应、彩色源骨架、轨迹、16 个控制器、目标偏移及基准/结果 Action 均恢复；重开后的第一次直接拖动也成功自动更新。
- 当前调节结果通过已有 Roblox 导出器序列化：73 帧、3 秒。仍未进行 Roblox Studio 内播放验收。

调节功能提供骨骼链 CCD IK 与 Reach 混合，不包含 Maya HumanIK 的全身平衡、Pull/Stiffness 或地面接触求解；不可达目标会显示残余距离。

1.1 最终播放观察：Blender 播放状态为 true，定时观察到时间轴已前进；错误状态为空。随后重新打开干净的 1.1 示例场景。
